<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the macOS build. Required for notarization
  (Phase 2). The library-validation exception lets Electron load our native
  addons (better-sqlite3, uiohook-napi, @paymoapp/active-window) which aren't
  signed with the same Team ID. Screen Recording is a TCC permission, not an
  entitlement, so it isn't listed here — it's requested at runtime.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
</dict>
</plist>
