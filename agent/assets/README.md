# Agent assets

Place the tray icon here as `tray.png` (recommended 16×16 / 32×32 for hi-dpi,
template image on macOS). The agent falls back to an empty icon if missing so it
still runs during development.
