# Agent environment (optional — normally provisioned by the installer)
# Backend the agent connects to:
TRACKLY_SERVER_URL=http://localhost:4000

# Zero-touch enrollment (set by the one-line installer; for manual/dev use):
# TRACKLY_SERVER=https://api.yourdomain.com
# TRACKLY_TOKEN=<employee enrollment token>
